# chronicle-external-recorder

Pivot from hook-based session logger to Chronicle: an external lossless recorder daemon (chronicled) with a thin watchdog/search plugin, layered storage (raw JSONL + markdown + SQLite/FTS), and derived cross-referenced narrative summaries.
